---
title: Writing
description: A collection of my writing on productivity and personal automation.
---
<div class="mb-5 ml-2 flex items-center justify-between">
  <h1 class="mb-2 text-3xl font-bold sm:mb-0">My writing</h1>

  <div>
    <a
      class="ml-2 inline-flex items-center rounded border border-transparent bg-gray-200 px-2.5 py-1.5 text-xs font-medium text-gray-700 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2"
      href="/feed/writing.xml"
    >
      <svg
        class="h-3 w-3"
        fill="currentColor"
        viewbox="0 0 20 20"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path d="M5 3a1 1 0 000 2c5.523 0 10 4.477 10 10a1 1 0 102 0C17 8.373 11.627 3 5 3z" />
        <path d="M4 9a1 1 0 011-1 7 7 0 017 7 1 1 0 11-2 0 5 5 0 00-5-5 1 1 0 01-1-1zM3 15a2 2 0 114 0 2 2 0 01-4 0z" />
      </svg>

      <p class="ml-1">RSS Feed</p>
    </a>
  </div>
</div>

{% assign featured_post = collections.writing.resources | where: 'featured', true | first %}

<a
  href="{{ featured_post.relative_url }}"
  class="flex overflow-hidden rounded-md bg-gray-800 bg-opacity-50 shadow hover:bg-gray-900 hover:bg-opacity-50"
>
  <img class="hidden h-48 w-56 object-cover lg:block" src="{{ featured_post.image }}">

  <div class="px-4 py-4">
    <p class="text-sm text-gray-300">
      <time datetime="{{ post.date }}">{{ featured_post.date | date: '%B %-d, %Y' }}</time>
    </p>

    <div>
      <p class="line-clamp-1 text-xl font-semibold text-gray-100" title="{{ featured_post.title }}">
        {{ featured_post.title }}
      </p>

      <p class="mt-3 line-clamp-2 text-base text-gray-300" title="{{ featured_post.description }}">
        {{ featured_post.description }}
      </p>
    </div>

    <div class="mt-3">
      <span class="text-base font-semibold text-teal-400"> Read full post </span>
    </div>
  </div>
</a>

<section class="mt-6 divide-y divide-gray-600 overflow-hidden rounded-md bg-gray-800 bg-opacity-80 shadow">
  <h2 class="bg-gradient-to-r from-gray-800 via-gray-800 to-blue-900 p-4 text-xl font-semibold">Productivity</h2>
  {% assign productivity_posts = collections.writing.resources
    | where: 'featured', false
    | where: 'category', 'Productivity'
  %}
  <ul class="divide-y divide-gray-600" role="list">
    {% for post in productivity_posts %}
      {% render 'post', post: post %}
    {% endfor %}
  </ul>
</section>
