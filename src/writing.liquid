---
title: Writing
description: A collection of my writing on productivity and personal automation.
---
{% assign productivity_posts = collections.writing.resources | where: 'category', 'Productivity' %}
<div class="rounded-lg bg-zinc-900 bg-opacity-60 py-24 sm:py-32">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl lg:max-w-4xl">
      <h2 class="text-3xl font-bold tracking-tight text-gray-100 sm:text-4xl">My writing</h2>
      <p class="mt-2 text-lg leading-8 text-gray-300">
        Learn how to automate, simplify, and improve your personal productivity.
      </p>

      <div class="mt-16 space-y-20 lg:mt-20 lg:space-y-20">
        {% for post in productivity_posts %}
          <a href="{{ post.relative_url }}" class="group relative isolate flex flex-col gap-8 lg:flex-row">
            <div class="aspect-[16/9 relative sm:aspect-[2/1] lg:aspect-square lg:w-64 lg:shrink-0">
              <img
                src="{{ post.image }}"
                alt="Image for '{{ post.title }}'"
                class="absolute inset-0 h-full w-full rounded-2xl bg-gray-50 object-cover ring-2 ring-white"
              >
              <div class="absolute inset-0 rounded-2xl ring-1 ring-inset ring-gray-100/10"></div>
            </div>
            <div>
              <div class="flex items-center gap-x-4 text-xs">
                <time datetime="{{ post.date }}" class="flex-shrink-0 text-gray-300">
                  {{- post.date | date: '%B %-d, %Y' -}}
                </time>
                <div class="flex items-center gap-x-2 overflow-x-hidden">
                  {% assign post_tags = post.tags | sort %}
                  {% for tag in post_tags %}
                    {% render 'post_tag', tag: tag %}
                  {% endfor %}
                </div>
              </div>
              <div class="relative max-w-xl">
                <h3 class="mt-3 text-lg font-semibold leading-6 text-gray-200 group-hover:text-gray-300">
                  <div>
                    <span class="absolute inset-0"></span>
                    {{ post.title }}
                  </div>
                </h3>
                <p class="mt-5 text-sm leading-6 text-gray-400">{{ post.description }}</p>
              </div>
            </div>
          </a>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
